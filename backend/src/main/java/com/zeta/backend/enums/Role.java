package com.zeta.backend.enums;

// Enum representing user roles in the system.
// USER: Regular user with access to user-level endpoints.
// ADMIN: Administrator with access to all endpoints including admin routes.

public enum Role {
    USER,
    ADMIN
}